var header_count_paragraph = document.getElementById(`paragraph-abount-count-country`);
var countries_count = countries_data.length
header_count_paragraph.textContent = `Currently, we have ${countries_count} countries`
